{
  "name": "smartcat",
  "private": true,
  "scripts": {
    "dev:api": "wrangler dev",
    "deploy:api": "wrangler deploy",
    "check:api": "wrangler deploy --dry-run"
  },
  "devDependencies": {
    "wrangler": "^4.136.3"
  }
}
